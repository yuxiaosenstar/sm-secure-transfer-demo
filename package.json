{
  "name": "sm-secure-transfer-demo",
  "version": "1.0.0",
  "description": "SM2/SM3/SM4 浏览器端文件加密传输与完整性校验演示(大文件分块 + Web Worker)",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch-path=server.js --watch-path=src server.js",
    "build": "vite build",
    "start": "NODE_ENV=production node server.js",
    "preview": "vite preview",
    "test": "node --test",
    "smoke": "node scripts/browser-smoke.mjs"
  },
  "dependencies": {
    "express": "^5.2.1",
    "sm-crypto": "^0.5.5",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
